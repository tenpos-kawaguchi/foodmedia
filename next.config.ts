import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'cms-tenpos.tenposfoodplace-hp.com',
      'www.tenpos.com',
      'cms-tenpos.tenposfoodplace-hp.com',
      'https://tools.itembox.design',
    ],
  },
};

export default nextConfig;
